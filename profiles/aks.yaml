kube-prometheus-stack:
  prometheus:
    azureIdentity:
      resourceID: ${prometheus.resource_id}
      clientID: ${prometheus.client_id}
    prometheusSpec:
      podMetadata:
        labels:
          aadpodidbinding: kube-prometheus-stack-prometheus
