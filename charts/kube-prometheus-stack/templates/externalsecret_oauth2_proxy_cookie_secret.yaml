apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Release.Name }}-oauth2-proxy-cookie-secret
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.secrets_names.cluster_secret_store }}
    kind: ClusterSecretStore
  target:
    name: {{ .Release.Name }}-oauth2-proxy-cookie-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      metadata:
        annotations:
          reloader.stakater.com/search: "true"
  dataFrom:
  - extract:
      key: {{ .Values.secrets_names.kube_prometheus_stack.oauth2_proxy_cookie_secret }}
